/**
 * Seed list for `/states/[stateSlug]` pages.
 *
 * Replace this with the 15 states Present Health currently serves.
 * - `name`: "Michigan"
 * - `slug`: "michigan" (used in URLs and the `State.slug` column)
 */
module.exports = {
    servedStates: [
        // { name: "Michigan", slug: "michigan" },
    ],
};

